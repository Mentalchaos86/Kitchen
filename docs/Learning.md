# Learning

HomeHub Learn uses local, deterministic pattern recognition.

It does not use cloud AI, location tracking or hidden profile changes.

Flow:

```text
Observe → Group → Calculate routine → Measure confidence → Detect deviation
```

The main screen only speaks when today's timing differs meaningfully from a reliable routine.
