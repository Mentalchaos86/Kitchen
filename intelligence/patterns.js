const KEY="homehub.aware.patterns";export function recordPatternEvent(event){if(!event)return;let h=[];try{h=JSON.parse(localStorage.getItem(KEY)||"[]")}catch{}h.push({id:event.id||event.title,title:event.title,type:event.type||event.intelligence?.type||"personal",weekday:new Date(event.start).getDay(),startHour:new Date(event.start).getHours(),recordedAt:new Date().toISOString()});localStorage.setItem(KEY,JSON.stringify(h.slice(-100)));}
